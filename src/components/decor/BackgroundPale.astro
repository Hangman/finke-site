---
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<div class={`relative w-full ${className || ""}`}>
  <div class="relative z-10 bg-[#EDF2FF]">
    <slot />
  </div>
</div>
